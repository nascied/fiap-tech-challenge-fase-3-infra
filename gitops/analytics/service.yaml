apiVersion: v1
kind: Service
metadata:
  name: analytics
  namespace: default
spec:
  selector:
    app: analytics
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8080
  type: ClusterIP

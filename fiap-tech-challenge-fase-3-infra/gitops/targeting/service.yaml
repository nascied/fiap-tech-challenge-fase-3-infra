apiVersion: v1
kind: Service
metadata:
  name: targeting
  namespace: default
spec:
  selector:
    app: targeting
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8080
  type: ClusterIP
